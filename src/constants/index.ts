import bannerData from './bannerData'
import servicesData from './servicesData'
import RouteName from './routes'
import Env from './env'
import LocalStorageKeys from './local-storage'

const Global = {
  DEFAULT_IMAGE: '/assets/illustration/profile-picture.png',
  MINIMUM_TOPUP: 10000,
  MAXIMUM_TOPUP: 1000000,
}

export { bannerData, servicesData, RouteName, Env, LocalStorageKeys, Global }
